const showStepsRevsStrokes = "Show Steps / Revs / Strokes";
const showStepsRevsStrokesTag = "show_steps_revs_strokes";
const showStepsRevsStrokesDefault = false;
const showStepsRevsStrokesDescription =
    "On: Steps / Revs / Strokes (sport dependent) is shown during workout. "
    "The metric is not persisted, saved or uploaded, only displayed. "
    "If the fitness machine does not transmit that metric then empty line is shown. "
    "Off: Steps / Revs / Strokes section is not shown.";
